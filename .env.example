# Scraper behaviour
HEADLESS=true
REQUEST_DELAY_SECONDS=2.5
MAX_RETRIES=3

# Cache
CACHE_ENABLED=true
CACHE_DIR=.cache

# Playwright browser (chromium, firefox, webkit)
PLAYWRIGHT_BROWSER=chromium

# Optional HTTP proxy (leave blank to disable)
HTTP_PROXY=
